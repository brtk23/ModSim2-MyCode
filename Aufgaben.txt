- Multigrid für singulär gestörte Probleme implementieren
    -> Kopplung in x oder y Richtung (im 5pkt-Stern)
    ist sehr klein, wodurch Glätter in die Richtung nicht glätten
    -> Restringieren nur entlang einer Achse 
    (die geschwächte Seite wird nicht restringiert)
    ODER unvollständige LR-Zerlegung 
    (sehr kleine Werte in A Matrix durch schwache Kopplung werden ignoriert)
    
- Multigrid solver to iterative solver integrieren 
- Gute OpenMP optimierung

Optional:
- Eigenwertzerlegung (Eigenwerte vor allem) von Matrizen berechnen mithilfe von den Lösern
    - det(A - I) = 0 LGS lösen.
- Berechne Spektralradius von S = I - M^-1*A, gucke ob < 1 wenn konvergiert oder >=1 wenn nicht
    - Spektralradius von S = größter Betrag vom Eigenwert der Matrix S
- Prüfe ob die Matrix Symmetrisch Positiv Definit (spd.) ist.
    - Matrix ist spd., wenn M = M^T und alle Eigenwerte positiv
    - Gucke dann bei Jakobi ob nicht konvergiert, wenn Dämpfungsfaktor kacke, also 0 < A < 2/omega * D nicht gilt.